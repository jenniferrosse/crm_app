<div class="container">
    
  
  <div class="row">
    <div class="col-md-5 col-md-offset-3 col-sm-7 col-sm-offset-1 col-xs-8 col-xs-offset-1">
      <%= link_to 'Edit', edit_broker_path(@broker) %> |
      <%= link_to 'Back', brokers_path %>
      <div class="well well-lg">
        <div class="row">
            
            <h2 class="text-left"><%= @broker.first_name %> <%= @broker.last_name %></h2>
          </p>
          </div>
        <%= @broker.email %><br>
        C <%= @broker.mobile %><br>
        D <%= @broker.direct_phone %><br>
        <br>
        <h4><%= @broker.company %></h4>
        <p><%= @broker.address %></p> 
      </div>

    </div>

  </div>


<div class="row">
  <h2>Clients</h2>
  <% if @clients.any? %>
    <% @clients.each do |commercial_lead| %>
      <%= link_to "#{commercial_lead.first_name} #{commercial_lead.last_name}", commercial_lead %><br>
    <% end %> 
  <% end %> 
</div>

</div>